kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-mysql-data-vol
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: demo-mysql
  volumeMode: Filesystem
  
