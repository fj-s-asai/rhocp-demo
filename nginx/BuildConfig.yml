kind: BuildConfig
apiVersion: build.openshift.io/v1
metadata:
  name: nginx
spec:
  strategy:
    type: Docker
    dockerStrategy: {}
  source:
    git:
      uri: https://github.com/yuu-ymt/rhocp-demo-nginx.git
      ref: master
    type: Git
    
  output:
    to:
      kind: ImageStreamTag
      name: 'nginx:latest'
  triggers:
    - type: ConfigChange
