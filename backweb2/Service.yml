kind: Service
apiVersion: v1
metadata:
  name: backweb2
  namespace: demo
  labels:
    app: backweb2
    service: backweb2
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: frontweb
  type: ClusterIP
  sessionAffinity: None
status:
  loadBalancer: {}

